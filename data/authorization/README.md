# Private derivative records

This directory is for server-side derivative manifests and other private processing records.

Do not place this directory under the public artifact route. The public portal serves files from `data/assets`; derivative manifests belong here so they can document how public web derivatives were produced without exposing preservation-master paths or processing details through `/files`.
